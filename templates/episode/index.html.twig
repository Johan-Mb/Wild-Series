{% extends 'base.html.twig' %}

{% block title %}Episode index{% endblock %}

{% block body %}
<div class="title-pages">
    <h1>Episode index</h1>
</div>

    <div class="content-category-series">

        <table class="table">
            <thead>
                <tr>
                    <th>Number</th>
                    <th>Title</th>
                    <th>Synopsis</th>
                    <th>actions</th>
                </tr>
            </thead>
            <tbody>
            {% for episode in episodes %}
                <tr>
                    <td>{{ episode.number }}</td>
                    <td>{{ episode.title }}</td>
                    <td>{{ episode.synopsis }}</td>
                    <td>
                        {# <a href="{{ path('program_showEpisode', {'program': program.slug, 'season':  episode.seasonId.id, 'episode': episode.slug}) }}">Voir l'épisode</a> #}
                        <a href="{{ path('episode_edit', {'slug': episode.slug}) }}">edit</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <a href="{{ path('episode_new') }}">Create new</a>
    </div>
{% endblock %}
