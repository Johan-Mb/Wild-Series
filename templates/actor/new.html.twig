{% extends 'base.html.twig' %}

{% block title %}New Actor{% endblock %}

{% block body %}
    <div class="title-pages">
        <h1>Create new Actor</h1>
    </div>

    {{ include('actor/_form.html.twig') }}

    <a class="content-category-series" href="{{ path('actor_index') }}">back to list</a>
{% endblock %}
