<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome bitch{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}

        {% include 'header.html.twig'%}

        {% block stylesheets %}

            {# On charge le CSS de bootstrap depuis le site directement #}
        {#     <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css"> #}

        {% endblock %}
            <link href="/assets/css/style.css" rel="stylesheet">
            <link rel="preconnect" href="https://fonts.googleapis.com">
            <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
            <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;900&family=Outfit:wght@400;700&display=swap" rel="stylesheet">
        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
{#
    {% for label, messages in app.flashes(['success', 'delete']) %}
        {% for message in messages %}
            <div class="flash-{{ label }}">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %} #}

    {% for message in app.flashes('success') %}
        <div class="flash-success">
            {{ message }}
        </div>
    {% endfor %}

    {% for message in app.flashes('delete') %}
        <div class="flash-delete">
            {{ message }}
        </div>
    {% endfor %}

    <body>
        {% block body %}

        {% endblock %}
    </body>
</html>
